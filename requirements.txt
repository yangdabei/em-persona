# Core ML stack. Heavy compute (model loading, fine-tuning, activation collection)
# runs in Colab; these are the versions the notebooks install there.
#
# unsloth is used ONLY for Experiment 1 training (src/training.py), mirroring the
# model-organisms-for-EM finetune pipeline. Experiment 2 and all analysis use plain
# transformers/peft.

# --- training (Exp 1) ---
unsloth==2025.5.1
trl==0.15.2

# --- model loading + inference (both experiments) ---
torch>=2.4.0
transformers>=4.49.0
peft>=0.14.0
accelerate>=1.0.0
bitsandbytes>=0.43.0

# --- data + geometry (all CPU float32) ---
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.4.0
pandas>=2.0.0

# --- viz ---
matplotlib>=3.8.0

# --- optional: OpenRouter judge harness (Exp 1 EM% quantification only) ---
openai>=1.40.0
python-dotenv>=1.0.0

# --- misc utilities used by reference conventions ---
jaxtyping>=0.2.0
tqdm>=4.66.0
pyyaml>=6.0
huggingface_hub>=0.25.0
tabulate>=0.9.0
